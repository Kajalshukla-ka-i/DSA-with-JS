<script>

    // Algorithm
// =================================================
document.write()
    // Input: An array of numbers, let's call it arr.

    // Output: The largest number in the array.

    // Initialize a variable:

    // Set largest to the first element of the array (arr[0]).
    // Iterate through the array:

    // Start a loop from index 1 to the end of the array.
    // For each element at index i:
    // Compare arr[i] with the current value of largest.
    // If arr[i] is greater than largest, update largest to be equal to arr[i].
    // Return the result:

    // After the loop is complete, largest will contain the largest number in the array.
    // Return the value of largest as the result.


</script>